# Configuración de Producción

# Database - Base de datos externa ya desplegada
# Reemplaza con los valores reales de tu servidor de base de datos:
# - DB_HOST: Dirección del servidor (ej: db.example.com o IP)
# - DB_PORT: Puerto (por defecto 5432 para PostgreSQL)
# - DB_USER: Usuario de la base de datos
# - DB_PASSWORD: Contraseña del usuario
# - DB_NAME: Nombre de la base de datos
DATABASE_URL="postgresql://DB_USER:DB_PASSWORD@DB_HOST:DB_PORT/DB_NAME?schema=public"

# JWT Secrets (Render auto-genera estos valores)
JWT_SECRET="your-jwt-secret-here"
JWT_REFRESH_SECRET="your-jwt-refresh-secret-here"

# Server
PORT=3000
NODE_ENV=production

# CORS (URL del frontend en Vercel)
FRONTEND_URL="https://your-app.vercel.app"
